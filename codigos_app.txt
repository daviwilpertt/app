varial = [] ou {}.filter((outra_variavel => {condicao}))  // filtra o objeto ou array com a condicao imposta, parecido com o for
inciar projeto no terminal // npm install vite@latest
abrir projeto // npm run dev
importar css // import './caminho_arquivo'
importar jsx // import nome_exportado from 'caminho_arquivo' 
React retorna so uma coisa, não pode retornar 2 h1
const [variavel, setVariavel] = React.useState() // como se fosse recarregar a pagina, dentro do parenteses coloca o valor inicial do estado,
const incrementar = () = > {setVariavel(condicao)}
matriz.concat(variavel)  // como se fosse um push no react, atualiza a matriz com o elemento variavel