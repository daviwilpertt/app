const brasil_copa = [1959, 1962, 1994, 2002]

for (let i = 0; i < brasil_copa.length; i++) {
    let msg = `o Brasa ganhou a copa do mundo no ano ${brasil_copa[i]}`
    console.log(msg)
    alert(msg)
}